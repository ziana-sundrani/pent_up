Unsent Project – anonymous message board with color moods, built on Next.js + Supabase.

## Quickstart

1) Copy env template and fill in Supabase keys

```bash
cp .env.local.example .env.local
```

Required vars

- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
- SUPABASE_SERVICE_ROLE_KEY (optional if you add RLS policies)

2) Create table in Supabase

```sql
create table if not exists messages (
  id bigint generated by default as identity primary key,
  to text not null,
  body text not null,
  color text not null default '#f0f0f0',
  created_at timestamp with time zone not null default now()
);
```

3) Install and run

```bash
npm install
npm run dev
```

Open http://localhost:3000.
